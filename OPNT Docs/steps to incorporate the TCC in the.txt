steps to incorporate the TCC in the STP/STD process and then adding it to the SFC

- createBOTDiscussion (cbd) is the base proc that converts a scrape to a discussion
- cbd is called by callSTDbyENTCELEBIND and callSTDbyIntCcode to handle the ENT/CELEb and non-ent discussions
- callSTDbyENTCELEBIND and callSTDbyIntCcode are called by STD_MANAGER
- When STD_MANAGER creates the discussions, the SFC pulls from OPN_POSTS based on matching tid, ccode and randomizing 

What do we want to see happen for NGA and other countries

- When the user belongs to the specific TCC's (currently only IND and USA - to be extended to NGA) we should pull the SFC for that Country Code or TCC
- that means, the SFC has to use the TCC when available and CCODE when TCC is not in (IND, USA, NGA)
- in order for that to happen, the discussions have to be created with TCC

The question is - should we shift the entire system to use TCC or should we continue to use CCODE and fork the Procs to decide for every proc - whether to use ccode or tcc ?

changing the basis to TCC is not practical for now - various reasons 
- it will take a long time to bring live content for all the bunch of countries

hence, we have to handle at the SFC level - to show live content for only USA, IND, NGA

in order to minimize the disruption to the existing system, we should create a separate repository (such as wsr_tcc) which will get data from the openAI python hookup

- then we should have a modified createBOTDiscussion - say, createSFCdiscussion - that will build discussion from the wsr_tcc - these will have to have some flag?
- 

- Let's first create the createSFCdiscussion for the NGA content we have in the wsr_tcc
- ok - found that first we have to deal with the OPN_MAIN_BOTS - do we create TCC-based bots ? or convert some of the bots for the specific TCC
